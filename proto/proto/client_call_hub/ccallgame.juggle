import common

module game{
	
	into_game ntf(int64 guid);
	
	play_order ntf(animal_game_info[] animal_info, skill skill_id);

	ready ntf();
	
	use_skill ntf(int64 target_guid, int16 target_animal_index);
	
	use_props ntf(props props_id, int64 target_guid, int16 target_animal_index);
	
	throw_dice ntf();

	choose_animal ntf(int16 animal_index);
	
	cancel_auto ntf();

	cancel_game req() rsp() err();
}
